package hash;

import java.security.MessageDigest;

public class qa4 {
public static void main(String[]args)
{
	/*
	 MessageDigest md = MessageDigest.getInstance("MD5");
	 byte[] digest = md.digest(psw.getBytes());
	 System.out.println(CryptoTools.bytesToHex(digest));
	 */
	
		// 5ae9b7f211e23aac3df5f2b8f3b8eada
	
	// A. For a
	/*
	 * Show (by presenting a logical argument) that it is impossible to determine the password.
	Show that it is nevertheless possible to find some pre-image of the hash.
	Estimate the time needed to find such a pre-image.
	Find such a pre-image. You can use an online password cracking tool.

	 */
}
}
